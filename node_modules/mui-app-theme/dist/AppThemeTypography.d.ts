import { CSSProperties } from "@material-ui/core/styles/withStyles";
import { IAppThemeTypography } from "./IAppThemeTypography";
export declare class AppThemeTypography implements IAppThemeTypography {
    private static readonly defaultFontFamily;
    private readonly fontFamily;
    50: CSSProperties;
    100: CSSProperties;
    175: CSSProperties;
    200: CSSProperties;
    300: CSSProperties;
    400: CSSProperties;
    700: CSSProperties;
    constructor(fontFamily?: string);
    protected createVariant(fontWeight: number, fontSize: number, lineHeight: string, letterSpacing?: number): CSSProperties;
}
