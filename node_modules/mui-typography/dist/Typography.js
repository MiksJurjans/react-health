import { __rest } from "tslib";
import React from "react";
import { ClassNameBuilder } from "./ClassNameBuilder";
const Typography = (props) => {
    const { children, size = 200, color = "medium", disabled = false, noWrap = false, bold = false, className, component, forwardedRef } = props, rest = __rest(props, ["children", "size", "color", "disabled", "noWrap", "bold", "className", "component", "forwardedRef"]);
    const classNameBuilder = new ClassNameBuilder(className);
    const typographyClassName = classNameBuilder
        .appendColor(disabled, color)
        .appendSize(size)
        .appendNoWrap(noWrap)
        .appendBold(bold)
        .build();
    const Component = component || "span";
    return (React.createElement(Component, Object.assign({ className: typographyClassName, ref: forwardedRef }, rest), children));
};
export { Typography };
//# sourceMappingURL=Typography.js.map