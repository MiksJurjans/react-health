import { makeStyles } from "@material-ui/styles";
const allSizes = [
    50,
    100,
    175,
    200,
    300,
    400,
    700,
];
export function sizeClassNameResolver(size) {
    return `mui-typography-size-${size}`;
}
function generateSizeStyles(theme) {
    const styles = {};
    const addSizeStyles = (size) => {
        styles[sizeClassNameResolver(size)] = theme.typography[size];
    };
    allSizes.forEach((size) => addSizeStyles(size));
    return styles;
}
export const useTypographySizeStyles = makeStyles((theme) => generateSizeStyles(theme));
//# sourceMappingURL=useTypographySizeStyles.js.map